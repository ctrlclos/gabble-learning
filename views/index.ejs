<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
  <title>Gabble Learning</title>
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container">
    <%- include('partials/flash') %>

    <div class="hero">
      <h1>Gabble Learning</h1>
      <p class="hero-subtitle">Learn vocabulary with AI-powered flashcards</p>

      <% if (isAuthenticated) { %>
        <p class="language-info">
          Learning <strong><%= currentUser.targetLanguage %></strong>
        </p>
        <div class="home-actions">
          <a href="/cards/new" class="btn btn-primary">Add Words</a>
          <a href="/cards" class="btn btn-secondary">My Cards</a>
        </div>
      <% } else { %>
        <div class="home-actions">
          <a href="/auth/register" class="btn btn-primary">Get Started</a>
          <a href="/auth/login" class="btn btn-secondary">Login</a>
        </div>
      <% } %>
    </div>

    <div class="features">
      <div class="feature">
        <h3>Enter Words</h3>
        <p>Type any word you want to learn in your target language.</p>
      </div>
      <div class="feature">
        <h3>AI Generates Cards</h3>
        <p>Get instant translations and definitions, or create your own.</p>
      </div>
      <div class="feature">
        <h3>Smart Review</h3>
        <p>Spaced repetition shows words right when you need to review them.</p>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
